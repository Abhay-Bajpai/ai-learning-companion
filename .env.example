# Global Environment Configuration
NODE_ENV=development

# Copy this file to backend/.env and configure the backend-specific variables
# The frontend will use environment variables from its own .env files
